{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Autonomous AI Computer Agent - WORKER",
  "scripts": {
    "test": "pytest tests/ -v",
    "test:unit": "pytest tests/unit/ -v",
    "test:integration": "pytest tests/integration/ -v"
  },
  "engines": {
    "node": ">=22"
  }
}
